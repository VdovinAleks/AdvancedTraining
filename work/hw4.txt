1)apt list nginx

1.1)apt install -y nginx 


apt remove nginx (удаление одного пакета)
apt autoremove (удаление всех пакетов (зависимостей))
apt purge nginx (удаление всего(вместе с конфигурацией))
apt show 'name'



dpkg -l
dpkg -i package_name  (install) 
dpkg -r package_name (remove)


2) wget https://download.virtualbox.org/virtualbox/7.0.4/virtualbox-7.0_7.0.4-154605~Ubuntu~jammy_amd64.deb

2.1)dpkg -i package_name
2.2)apt -f -y install

3)snap install package_name  (install)
snap refresh package_name (download)
3)snap remove package_name  (delete)
snap list (list)

4)      3**** db  cd / && mkdir directory && touch file

crontab -l  содержимое
crontab -r удаление текущего файла расписание
crontab -e  редактирование
sudo crontab -u username работа с файлом расписания другого пользователя
